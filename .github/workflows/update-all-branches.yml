name: Update All Branches
on:
  workflow_dispatch:
  schedule:
    # Run daily at 2 AM UTC
    - cron: '0 2 * * *'

permissions:
  contents: write

jobs:
  update-branches:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Update All Branches from Upstream
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # --- CONFIGURATION ---
          UPSTREAM_URL="https://github.com/frappe/frappe.git"

          echo "🚀 Starting Branch Update Process..."

          git config --global user.name 'GitHub Actions Bot'
          git config --global user.email 'actions@github.com'

          # 1. SETUP UPSTREAM
          echo "🔗 Setting up upstream repository..."
          git remote remove upstream 2>/dev/null || true
          git remote add upstream $UPSTREAM_URL
          git fetch upstream

          # 2. IDENTIFY TARGET BRANCHES
          echo "🔍 Identifying branches to update..."
          TARGET_BRANCHES=$(git branch -r | grep 'upstream/' | grep -v 'HEAD' | \
            grep -E 'upstream/develop$|upstream/version-[0-9]+$|upstream/v[0-9]+$' | \
            sed 's/upstream\///g')

          if [ -z "$TARGET_BRANCHES" ]; then
            echo "⚠️ No target branches found to update"
            exit 0
          fi

          echo "📋 Branches to update:"
          echo "$TARGET_BRANCHES"

          # 3. UPDATE EACH BRANCH
          for BRANCH in $TARGET_BRANCHES; do
             # Trim whitespace
             BRANCH="${BRANCH#"${BRANCH%%[![:space:]]*}"}"
             BRANCH="${BRANCH%"${BRANCH##*[![:space:]]}"}"
             if [ -z "$BRANCH" ]; then continue; fi

             echo ""
             echo "🔧 Processing branch: $BRANCH"

             # Clean working directory
             git reset --hard
             git clean -fd

             # Check out the upstream branch
             echo "  ↓ Fetching latest from upstream/$BRANCH..."
             git checkout -B $BRANCH upstream/$BRANCH

             # Push to origin
             echo "  ↑ Pushing to origin/$BRANCH..."
             git push -f origin $BRANCH

             echo "  ✅ Branch $BRANCH updated successfully"
          done

          echo ""
          echo "🎉 All branches updated successfully!"
